# Web Framework
fastapi>=0.120.1
uvicorn[standard]>=0.32.0

# Pydantic for data validation
pydantic==2.9.2

# AWS SDK for Python (Boto3) - for AgentCore Gateway integration
# Requires boto3 >= 1.40.0 for bedrock-agentcore support
boto3>=1.40.62

# AgentCore SDK - for OAuth2 identity management
# Requires >= 1.0.0 for identity.auth module support
bedrock-agentcore>=1.0.0

# HTTP Client
requests==2.32.3
